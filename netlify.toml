# Netlify configuration for Inkwell unified deployment
# One site serves BOTH the landing at / and the real Inkwell web app at /inkwell/app/
# The app bundle lands in dist/inkwell/app during the build process.

[build]
  command = "bash build.sh"
  publish = "dist"

[build.environment]
# The Supabase publishable key is intentionally shipped in the browser bundle.
# Keep secret scanning enabled for every other configured value.
SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_PUBLISHABLE_KEY"

# Real Inkwell web app SPA fallback (static assets in /inkwell/app/ are served directly first)
[[redirects]]
  from = "/inkwell/app/*"
  to = "/inkwell/app/index.html"
  status = 200

# Canonical landing URL redirect
[[redirects]]
  from = "/inkwell"
  to = "/inkwell/"
  status = 301

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  # The service worker must never be cached by the browser, or updates stall.
  for = "/inkwell/app/sw.js"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/inkwell/app/manifest.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json"

[[headers]]
  # Never let the app shell stay pinned to an older hashed JavaScript bundle.
  for = "/inkwell/app/"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
    CDN-Cache-Control = "no-store"

[[headers]]
  for = "/inkwell/app/index.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
    CDN-Cache-Control = "no-store"

[[redirects]]
  # Approved dedicated production Worker target for session and entitlement routes.
  from = "/api/auth/session"
  to = "https://inkwell-entitlement-worker.teachtylerhenley.workers.dev/api/auth/session"
  status = 200
  force = true

[[redirects]]
  from = "/api/entitlement"
  to = "https://inkwell-entitlement-worker.teachtylerhenley.workers.dev/api/entitlement"
  status = 200
  force = true

[[redirects]]
  from = "/api/entitlement/prime"
  to = "https://inkwell-entitlement-worker.teachtylerhenley.workers.dev/api/entitlement/prime"
  status = 200
  force = true

[[redirects]]
  from = "/api/entitlement/settle"
  to = "https://inkwell-entitlement-worker.teachtylerhenley.workers.dev/api/entitlement/settle"
  status = 200
  force = true

[[redirects]]
  from = "/api/entitlement/recover"
  to = "https://inkwell-entitlement-worker.teachtylerhenley.workers.dev/api/entitlement/recover"
  status = 200
  force = true

[[redirects]]
  # Approved dedicated production Worker target for catalog and capability reads.
  from = "/api/catalog"
  to = "https://inkwell-entitlement-worker.teachtylerhenley.workers.dev/api/catalog"
  status = 200
  force = true

[[redirects]]
  from = "/api/capabilities"
  to = "https://inkwell-entitlement-worker.teachtylerhenley.workers.dev/api/capabilities"
  status = 200
  force = true
